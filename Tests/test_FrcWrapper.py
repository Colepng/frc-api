import src.FrcWrapper as FrcWrapper

def test_Rankings_info():
    # """Tests a API call to get the rankings of a event"""

    # rankings_instance = FrcWrapper.Rankings("frc254")
    # response = rankings_instance.info()

    # assert isinstance(response, dict)
    # assert response['rankings'][0]['team_key'] == 'frc254'
    print(FrcWrapper.test.add(1, 2))
